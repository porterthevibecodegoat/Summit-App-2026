# Content Import Guide

Approved event content should be imported into canonical Supabase tables, then exported into a schema-validated mobile snapshot.

Demo labels are centralized in `packages/test-fixtures`; replace them with approved content through seed/import workflows, not scattered component edits.

CSV/JSON templates will be added with the Phase 1 import workflow.
